<!-- rounded_corner_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/lightgray" /> <!-- Adjust the background color as needed -->
    <corners android:radius="8dp" /> <!-- Adjust the corner radius as needed -->
    <stroke
        android:width="1dp"
    android:color="@android:color/darker_gray" /> <!-- Adjust the border color as needed -->
</shape>
